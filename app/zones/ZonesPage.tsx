import { IZone } from "@/src/interfaces/zone"

type Props = {
    zones: IZone[]
}
const ZonesPage = ({ zones }: Props) => {
    return (
        <div>ZonesPage</div>
    )
}

export default ZonesPage